spring:
  application:
    name: cruddemo
  datasource:
    url: jdbc:mysql://localhost:3306/hb-03-one-to-many
    username: springstudent
    password: springstudent
  main:
    banner-mode: off
logging:
  level:
    root: warn
    org:
      hibernate:
        SQL: trace
        orm:
          jdbc:
            bind: trace